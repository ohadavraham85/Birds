{
  "name": "birds-functions",
  "version": "1.0.0",
  "description": "Firebase Cloud Functions for יומן צפרות — server-side AI voice-observation parsing (keeps the Anthropic API key off the client).",
  "type": "module",
  "engines": { "node": "20" },
  "main": "lib/index.js",
  "scripts": {
    "build": "tsc",
    "deploy": "npm run build && firebase deploy --only functions"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.32.1",
    "firebase-admin": "^12.6.0",
    "firebase-functions": "^6.1.1"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
