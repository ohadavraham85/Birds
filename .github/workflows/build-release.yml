name: Build & Release

on:
  push:
    branches: [claude/bird-observation-pwa-kqedtw, main]
    paths-ignore:
      - '**.md'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build-release:
    runs-on: ubuntu-latest
    outputs:
      version: ${{ steps.ver.outputs.version }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Type-check
        run: npm run typecheck

      # Reserves the version number by tagging+pushing it immediately, before
      # the (much slower) build even starts — two runs triggered close
      # together otherwise both compute the same "next" version from the same
      # stale tag list and race to push it, and whichever loses gets its tag
      # push rejected, which fails that whole job and skips deploy-pages
      # (needs: build-release) even though the build itself was fine. Retries
      # against a freshly-fetched tag list on conflict instead.
      - name: Claim next version
        id: ver
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          for attempt in $(seq 1 8); do
            git fetch origin --tags --force --quiet
            latest=$(git tag -l 'v*' --sort=-v:refname | head -1)
            if [ -z "$latest" ]; then
              VER="2.0.0"
            else
              IFS=. read -r major minor patch <<< "${latest#v}"
              if [ "$major" -lt 2 ]; then
                VER="2.0.0"
              else
                VER="$major.$minor.$((patch + 1))"
              fi
            fi
            git tag "v$VER"
            if git push origin "v$VER"; then
              echo "Claimed v$VER (previous: ${latest:-none})"
              echo "version=$VER" >> "$GITHUB_OUTPUT"
              exit 0
            fi
            echo "v$VER was claimed by a concurrent run — retrying with a fresh version…"
            git tag -d "v$VER"
            sleep $((RANDOM % 4 + 1))
          done
          echo "::error::Could not claim a unique version tag after 8 attempts"
          exit 1

      - name: Build (Vite + PWA)
        env:
          GITHUB_PAGES: '1'
        run: npx vite build

      - name: Stamp version + package
        run: |
          VER="${{ steps.ver.outputs.version }}"
          echo "{\"version\": \"$VER\", \"builtAt\": \"$(date -u +%FT%TZ)\", \"commit\": \"$GITHUB_SHA\"}" > dist/version.json
          cd dist
          zip -qr "../birds-app-v$VER.zip" .
          ls -la ../*.zip

      - name: Upload Pages build
        uses: actions/upload-artifact@v4
        with:
          name: dist
          path: dist

      - name: Create release
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          VER="${{ steps.ver.outputs.version }}"
          NOTES=$(printf 'בנייה אוטומטית (Vite + TypeScript, PWA offline-first) מ-commit %s.\n\nהאפליקציה מותקנת מ-GitHub Pages; ה-ZIP הוא ה-build הסטטי (dist) לפריסה בכל אחסון סטטי עם HTTPS.' "${GITHUB_SHA::7}")
          gh release create "v$VER" "birds-app-v$VER.zip" \
            --title "יומן צפרות v$VER" \
            --notes "$NOTES"

  deploy-pages:
    needs: build-release
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pages: write
      id-token: write
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Download build
        uses: actions/download-artifact@v4
        with:
          name: dist
          path: site

      - name: Configure Pages
        uses: actions/configure-pages@v5
        with:
          enablement: true

      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: site

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
